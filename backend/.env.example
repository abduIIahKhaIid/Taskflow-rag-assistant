# This file contains placeholders only.
# Empty external-service variables are expected during Phase 2.

APP_NAME=TaskFlow RAG API
APP_ENVIRONMENT=development
APP_VERSION=0.1.0
API_V1_PREFIX=/api/v1
FRONTEND_ORIGIN=http://localhost:3000
LOG_LEVEL=INFO

# GROQ_API_KEY must remain server-side.
GROQ_API_KEY=
GROQ_MODEL=

SUPABASE_URL=
SUPABASE_PUBLISHABLE_KEY=
# SUPABASE_SECRET_KEY must remain server-side.
SUPABASE_SECRET_KEY=
DATABASE_URL=
